#!/usr/bin/env python3
""" Function that handles concat"""
def concat(str1: str, str2: str) -> str:
    """
    Attributes: str1(str) str2(str) string to convert

    Returns: the add string
    """
    return str1 + str2
